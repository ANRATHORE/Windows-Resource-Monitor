# Windows-Resource-Monitor
Lightweight system monitor with adaptive anomaly detection. Tracks CPU, RAM and disk every 5s, builds a 7-day sliding window profile, and alerts only when 3 consecutive readings breach dynamic Normal/Warning/Critical zones derived from your actual usage history.
